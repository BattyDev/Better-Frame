{
  "name": "better-frame-monorepo",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev -w better-frame",
    "build": "npm run build -w better-frame",
    "test": "npm test -w better-frame",
    "lint": "npm run lint -w better-frame",
    "format": "npm run format -w better-frame"
  }
}
